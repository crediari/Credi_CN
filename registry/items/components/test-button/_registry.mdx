---
name: test-button
type: registry:ui
title: Test Button
description: A standalone emerald test button with no shadcn button dependency.
---

This item installs only `test-button.tsx`. It does not pull the default shadcn `button`.

```tsx
import { TestButton } from "@/components/ui/test-button";

<TestButton label="Olá do registry" />
```
