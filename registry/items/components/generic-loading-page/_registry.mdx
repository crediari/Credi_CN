---
name: generic-loading-page
type: registry:component
title: Generic Loading Page
description: Full-page loading state with spinner and logo.
files:
  - path: generic-loading-page.tsx
    type: registry:component
---

Use while a route or query is pending. Pass `logoSrc` if the app logo is not at `/logo.svg`.

```tsx
import { GenericLoadingPage } from "@/components/generic-loading-page";

<GenericLoadingPage message="Carregando contratos..." />
```
