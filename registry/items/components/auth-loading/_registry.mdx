---
name: auth-loading
type: registry:component
title: Auth Loading
description: Full-screen auth loading overlay with logo.
dependencies:
  - motion
files:
  - path: auth-loading.tsx
    type: registry:component
---

Pass `isLoading` and an optional message. Defaults to `/logo.svg`.

```tsx
import { AuthLoading } from "@/components/auth-loading";

<AuthLoading isLoading message="Entrando..." />
```
